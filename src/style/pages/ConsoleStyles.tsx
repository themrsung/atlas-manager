import styled from "styled-components"

export default class ConsoleStyles {
   static Wrap = styled.div`
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
   `
}
